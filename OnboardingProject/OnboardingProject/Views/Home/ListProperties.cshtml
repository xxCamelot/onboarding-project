@model System.Collections.Generic.List<PropertyDataModel>

@{
    ViewData["Title"] = "List all Properties";
}

<h1>@ViewData["Title"]</h1>

<p>On this page you will see all current properties.</p>

@foreach (var item in Model)
{
    <div>
        @Html.ActionLink(item.Address, "PropertyDescription", "Home", new {propertyID = item.Id })
        <hr />
    </div>
}