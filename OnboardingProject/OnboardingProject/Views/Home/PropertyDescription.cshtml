@model PropertyDescriptionModel

@{
    ViewData["Title"] = "Property Description";
}

<h1>@ViewData["Title"]</h1>

<p>Here is all available info on the selected property.</p>

<div>
    @Html.ActionLink(@Model.property.address, "UpdateProperty", "Home", new { propertyID = Model.property.propertyID, index = 1 })
    <hr />
</div>
<div>
    @Html.ActionLink(@Model.property.description, "UpdateProperty", "Home", new { propertyID = Model.property.propertyID, index = 2 })
    <hr />
</div>
<div>
    @Html.ActionLink(@Model.property.surface.ToString(), "UpdateProperty", "Home", new { propertyID = Model.property.propertyID, index = 3 })
    <hr />
</div>
<div>
    @Html.ActionLink(@Model.property.services, "UpdateProperty", "Home", new { propertyID = Model.property.propertyID, index = 4 })
    <hr />
</div>

<p>Rooms in the property:</p>

@foreach(var item in Model.rooms)
{
    <div>
        @Html.ActionLink(item.description, "RoomDescription", "Home", new { roomID = item.roomID })
        <hr />
    </div>

}